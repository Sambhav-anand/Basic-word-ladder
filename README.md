# Basic-word-ladder
Interactive Word Ladder web app built with HTML, Tailwind CSS, and JavaScript. Uses BFS algorithm to compute the shortest word transformation sequence with a stylish terminal-themed interface.
